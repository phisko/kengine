# [registerMove](registerMove.hpp)

```cpp
template<typename ... Comps>
void registerMove() noexcept;
```

Registers a standard implementation of the [Move](../../components/meta/Move.md) `meta Component` for each type in `Comps`.