# [registerCount](registerCount.hpp)

```cpp
template<typename ... Comps>
void registerCount() noexcept;
```

Registers a standard implementation of the [Count](../../components/meta/Count.md) `meta Component` for each type in `Comps`.