#pragma once

// stl
#include <string>
#include <vector>

namespace kengine::render::animation {
	//! putils reflect all
	//! class_name: animation_files
	struct files {
		std::vector<std::string> list;
	};
}

#include "files.rpp"