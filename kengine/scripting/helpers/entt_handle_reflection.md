# [entt_handle_reflection](entt_handle_reflection.hpp)

`putils reflection` API for `entt::handle`. This is the type used to represent entities in scripting languages, and bindings are done to that type.