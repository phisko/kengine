#pragma once

namespace kengine {
	// Indicates that the TextureModelComponent<T> should be initialized with the data in TextureDataComponent
	template<typename T>
	struct TextureLoaderComponent {};
}