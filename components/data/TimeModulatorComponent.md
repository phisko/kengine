# [TimeModulatorComponent](TimeModulatorComponent.hpp)

`Component` that lets `Entities` be used to modulate time.

## Members

### factor

```cpp
float factor = 1.f;
```

Factor by which delta time should be multiplied each frame.