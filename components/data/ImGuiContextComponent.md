# [ImGuiContextComponent](ImGuiContextComponent.hpp)

`Component` holding a pointer to an `ImGuiContext`. This is useful, among other things, when plugins wish to use ImGui (as the `ImGuiContext` is dll-specific).